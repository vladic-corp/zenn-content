---
title: "AIエージェントに日本の法人データを売ってみた — x402で作るAPI事業、構想から初売上まで48時間"
emoji: "🏢"
type: "tech"
topics: ["x402", "ai", "cloudflare", "web3", "個人開発"]
published: true
---

新潟でホームページ制作をやっている個人開発者です。先日、**AIエージェント向けに日本の法人データを従量課金で売るAPI**を作って公開したところ、掲載から24時間以内に海外のエージェントが4回支払ってくれました。金額は$0.019。笑ってしまう額ですが、「人間が一度も介在しない販売」が成立した瞬間でした。

この記事では、市場調査から初売上までの48時間でやったこと・学んだことをまとめます。

## x402とは何か（30秒版）

[x402](https://x402.org)は、HTTPの`402 Payment Required`を使った決済プロトコルです。エージェントがAPIを叩く→サーバーが402と価格を返す→エージェントのウォレットがUSDCで署名→再送→データが返る。**APIキーもアカウント登録も不要**で、「支払うウォレット＝アカウント」になります。Coinbaseが主導し、現在はLinux Foundation傘下。AWS・Google・Visa等40社が参加しています。

## 作る前に市場を実測した

x402のマーケットプレイス[x402scan](https://www.x402scan.com)は、裏でtRPCの公開APIが動いています。これを直接叩いて、直近30日の売上ランキング・買い手の行動・新規参入の成績を全部数えました。分かったことは：

- ネットワーク全体で月$85万。ただし約2割は自己取引（wash）、2割はLLM推論ゲートウェイ1社
- **実需のコアは月$5〜8万**。小さい。でも買い手は13万ウォレットいて売り手は数百＝圧倒的な供給不足
- 勝ちパターンは「ニッチ縦切りデータ」。助成金データAPIが3週間で買い手162、中国の風水データAPIですら12日で買い手189
- **日本の業務データはほぼ空白**（後述しますが、完全な空白ではありませんでした）

「日本の法人データ」を選んだのはこの実測からです。

## 作ったもの：Kaisha API

https://kaisha-api.hp-vladic.workers.dev

- **法人解決** `/resolve` — 社名（表記ゆれ吸収）or 法人番号13桁 → 正式名称・所在地・法人種別・存続状態。国税庁の法人番号公表データ**全576万社**
- **インボイス照合** `/invoice/verify` — 適格請求書の登録番号（T+13桁）を国税庁の公表データ**505万件**と実在照合。これは**x402上で世界唯一**のはず（LLMには原理的にできない決定的検証です）
- **営業日・祝日** `/calendar/*` — 内閣府データで営業日計算
- 価格は1コール$0.002〜0.01。USDCで**BaseとSolana両対応**

スタックはCloudflare Workers + Hono + D1。決済は`@x402/hono`にミドルウェアを1個挟むだけです。

## ハマりどころ（ここが本題）

### D1のSQL文上限は「10万バイト」

576万行のINSERTを流し込むとき、500行ずつに分割したのに`SQLITE_TOOBIG`。原因は**文字数ではなくUTF-8バイト数**の上限でした。日本語は1文字3バイトなので、文字数カウントは罠です。80KBバイトで切って解決。

### D1無料枠は「1DBあたり500MB」

全国データは約1GB。月$5のWorkers Paidにすれば済む話ですが、意地で**6DB分割**（法人×4・インボイス×2）にして固定費ゼロを維持しました。シャードキーは「番号の末尾2桁 % N」。末尾1桁だと10種類を4分割することになり30/30/20/20に偏るので、2桁（00-99）を使うのがポイントです。

### Solanaの売り手は「ATA」を作っておかないと全決済が落ちる

これが一番の学びでした。x402でSolana対応を宣言しても、**受け取りウォレットにUSDCのトークン口座（ATA）が無いと、全部の支払いがオンチェーンシミュレーションで失敗します**（`InstructionError: InvalidAccountData`）。エラーメッセージからは原因が読み取りにくいので、Solana対応する売り手は最初に自分宛てにUSDCを1円分送るか、ATAを明示的に作成しておきましょう。

### x402scanへの掲載は `/openapi.json` が必須

`.well-known/x402`は旧式扱いで、いまは**OpenAPI文書をオリジン直下に置く**のが登録要件です。無料エンドポイントには`"security": []`を付けないと「402を返さない」と怒られます。登録すると5エンドポイントを1本ずつ実際に402ハンドシェイクで検証されます（ちゃんとしてる）。

## 初売上の中身

x402scan掲載から24時間以内に、海外のウォレットが**安い順に全エンドポイントを1回ずつ**買っていきました。調べると、月$290を10以上のAPIに払い続けている「新着APIを評価して回るエージェント運用者」。エージェント経済の買い手は、こうやって自動的に新商品を見つけて試すのだと実感しました。

## 正直な現実と、それでもやる理由

- 市場はまだ小さく、類似の先行者もいました（法人番号照会のAPIが2ヶ月先行・買い手44）。「空白市場」は幻想でした
- それでも**インボイス照合・営業日計算・Solana対応・MCP配布**は現時点でうちだけ。ニッチの中でさらに縦に切る余地は十分あります
- 固定費0円・限界費用ほぼ0円なので、これは「エージェント経済が来るかどうか」への**極めて安いコールオプション**です

## エージェントから使う方法

MCP対応クライアント（Claude Code等）なら1行です：

```bash
claude mcp add kaisha -e EVM_PRIVATE_KEY=0x<USDC入りBase財布> -- npx -y kaisha-mcp
```

ソースはこちら：https://github.com/vladic-corp/kaisha-api

質問・感想歓迎です。特に「エージェントに買わせたい日本のデータ」のアイデアがあれば聞きたいです。
